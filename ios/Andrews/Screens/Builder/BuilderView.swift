import SwiftUI

struct BuilderView: View {
    var body: some View {
        Text("Build meal")
            .font(.display(34))
            .foregroundStyle(Color.ink)
            .navigationTitle("Build meal")
    }
}
