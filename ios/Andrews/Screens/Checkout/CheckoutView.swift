import SwiftUI

struct CheckoutView: View {
    var body: some View {
        Text("Checkout")
            .font(.display(34))
            .foregroundStyle(Color.ink)
            .navigationTitle("Checkout")
    }
}
